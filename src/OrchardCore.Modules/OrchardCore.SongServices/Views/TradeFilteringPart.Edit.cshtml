@* As you can see the model here is the view model object this time not a ShapeViewModel<T>. *@

@model TradeFilteringPartViewModel
@using OrchardCore.SongServices.ViewModels;

@* Nothing special here regarding the editor fields. However, notice that the Biography (ContentField) editor is not
    here, because (as mentioned) it has its own editor and display shape so no need to worry about that. *@

<fieldset class="form-group" asp-validation-class-for="Buyer">
	<label asp-for="Buyer">@T["Buyer"] <span asp-validation-for="Buyer"></span></label>
	<input asp-for="Buyer" class="form-control" />
	<span class="hint">@T["Buyer's name"]</span>
</fieldset>
<fieldset class="form-group" asp-validation-class-for="Seller">
	<label asp-for="Seller">@T["Seller"] <span asp-validation-for="Seller"></span></label>
	<input asp-for="Seller" class="form-control" />
	<span class="hint">@T["Seller's name"]</span>
</fieldset>


@* NEXT STATION: Go back to Drivers/PersonPartDisplayDriver and find the UpdateAsync method. *@
